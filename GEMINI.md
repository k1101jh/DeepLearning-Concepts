# 프로젝트 규칙

## 1. 개념 정리 기본 원칙
- 개념 리뷰 및 정리는 항상 하나의 개념당 하나의 문서 파일로 관리한다.
- 파일 및 폴더는 다음 형식 중 하나로 통일하여 저장한다.
  - `{카테고리}/{개념명}/{개념명}.md` (예: `Diffusion Model/Diffusion Model/Diffusion Model.md` 또는 `Diffusion Model/Diffusion Model.md`)
  - 또는 `{카테고리}/{개념명}/concept.md` (예: `Optimizer/AdamW/concept.md`)
  - 이 프로젝트에서는 개념별 이미지 및 코드 자산 관리를 위해 개념마다 독립 폴더를 생성하는 구조를 기본 표준으로 한다.
- 폴더와 파일 이름은 다음 규칙을 따른다.
  - 개념 제목에 포함된 `:` , `/` , `?` , `*` , `<` , `>` , `|` 같은 문자는 `_`로 치환한다.
  - 이미 동일한 개념에 대한 정리 폴더/파일이 존재하면 새 폴더를 만들지 않고 기존 정리 파일을 업데이트한다.
  - 가능하면 저장소의 기존 카테고리 구조(`Basics`, `Optimizer`, `Training`, `Transformer`, `Diffusion Model`, `LLM`, `Vision`, `Data` 등)를 그대로 활용하며, 필요시 새 카테고리를 추가한다.

## 2. 개념 정리 작성 템플릿
개념 정리는 딥러닝/AI 개념을 직관적이고 이해하기 쉽게 구조적으로 정리하는 것을 목표로 한다.
문서 상단에는 참고한 공식 문서, 대표 논문, 튜토리얼 등의 레퍼런스를 먼저 명시한다.

```markdown
# {개념명}

---
Reference:
- [{논문/문서 제목}]({URL})
- [{관련 링크/블로그 제목}]({URL})

---

## 1. 개념 정보 및 한 줄 요약
- **개념명**: 
- **관련 분야/카테고리**: 
- **한 줄 요약**: 이 개념이 무엇이고 어떤 역할을 하는지 한 문장으로 요약한다.

## 2. 등장 배경 및 해결하려는 문제 (Why?)
- 기존 방식이나 알고리즘이 가졌던 한계점과 문제점을 서술한다.
- 이 개념이 도입됨으로써 해결하고자 하는 핵심 목표를 명확히 적는다.

## 3. 핵심 원리 및 메커니즘 (How?)
- 핵심 작동 방식과 아이디어를 직관적으로 이해하기 쉽게 정리한다.
- 주요 수식($...$ 또는 $$...$$)을 포함하며, 수식에 사용된 각 기호와 변수의 의미를 명확히 설명한다.
- 필터/레이어/알고리즘의 동작 순서나 처리 흐름을 단계별로 기술한다.

## 4. 핵심 세부 개념 및 부가 설명
- 개념을 이해하는 데 필요한 연관 보충 용어나 핵심 서브 메커니즘(예: Markov chain, EMA 등)을 세부 항목으로 나누어 설명한다.
- 어려운 용어나 복잡한 구조는 2~5문장 정도의 쉬운 직관적 비유와 함께 부가 설명을 덧붙인다.

## 5. 코드 구현 예시 (PyTorch / Python)
- 해당 개념을 직관적으로 이해할 수 있는 PyTorch 또는 Python 구현 예시 코드를 작성한다.
- **코드 작성 필수 규칙**:
  - 함수 및 클래스에는 한글 베이스의 **docstring**을 작성하고, 파라미터 및 반환값의 **타입(typing)**과 설명을 명확히 기재한다.
  - 코드 주요 로직에는 한글 주석을 추가하여 동작 과정을 쉽게 이해하도록 돕는다.

## 6. 장단점 및 기존 개념과의 비교
- 기존 방식(또는 타 유사 개념)과의 차이점을 비교 표(Table) 등으로 정리한다.
- 장점과 한계점(단점 및 고려해야 할 점)을 구분하여 기술한다.

## 7. 활용 사례 및 응용
- 실제 딥러닝 모델, 라이브러리, 또는 산업계/연구에서 이 개념이 어떻게 적용되는지 대표 사례를 제시한다.
```

### Figure 및 Table(표) 작성 규칙
- 개념 이해를 돕는 **Figure(그림)**와 **Table(표)**를 적극 활용한다.
- **이미지 첨부 방식**:
  1. **온라인 이미지 URL 사용**: 웹상의 공식 이미지/Diagram URL (`https://...`)을 그대로 사용할 수 있다. 이 경우 캡션 및 본문/상단 레퍼런스(Reference)에 출처를 명확히 명시한다.
  2. **로컬 이미지 저장**: 로컬 이미지를 사용할 경우 해당 개념 폴더 내 `images/` 디렉터리에 저장한다. (예: `{카테고리}/{개념명}/images/Fig 1.png`)
- 리뷰 파일(`.md`) 작성 시 본문의 관련 위치에 아래와 같은 마크다운 이미지 링크 및 인용구 캡션 형태로 첨부한다.
  ```markdown
  ![alt text](./images/Fig%201.png)
  > **Figure 1. 캡션 설명 (출처: [Reference Link](https://...))**

  ![alt text](https://example.com/diagram.png)
  > **Figure 2. 온라인 이미지 캡션 설명 (출처: Reference)**
  ```

## 3. 검증 및 품질 관리
1. 내용 및 근거 검증 (환각 방지)
   - 개념 정의, 원리, 수식, 레퍼런스 링크가 정확한지 확인한다.
   - 존재하지 않는 수식이나 왜곡된 사실(hallucination)을 임의로 창작하는 것을 엄격히 금지한다.
   - 공식 문서, 원 논문, 표준 교재에 기반하여 사실에 입각한 내용만 기술한다.
2. 직관적 가독성 검증
   - 지나치게 난해한 학술 용어 나열을 피하고, 서브 불렛 포인트(`-`, `  -`, `    -`)를 활용해 계층적으로 서술한다.
   - 문단을 적절히 나누어 가독성을 높인다.
3. 코드 작성 및 typing/docstring 검증
   - Python/PyTorch 코드 작성 시 typing 오류가 없는지 확인한다.
   - docstring과 주석이 한글 베이스로 작성되었는지, 입출력 타입 설명이 빠짐없이 들어갔는지 검증한다.
4. 개념 상호 참조 및 연결성 검증
   - 이미 repository 내에 정리된 연관 개념이 있다면 중복 설명을 피하고 해당 문서 링크(`[개념명](../{카테고리}/{개념명}.md)`)를 걸어 연동한다.
   - 새로 추가된 개념인 경우 핵심 내용을 명확히 기술한다.

## 4. 작성 완료 후 반드시 수행할 작업
개념 정리 파일을 작성/수정한 뒤에는 아래 작업을 순서대로 수행한다.
1. README 업데이트
   - 새 개념이 추가되면 `README.md` 해당 카테고리 목록에 해당 개념 정리 링크를 추가한다.
   - 상대 경로 예시:
     - `[Diffusion Model](./Diffusion%20Model/Diffusion%20Model.md)` 또는 `[Diffusion Model](./Diffusion%20Model/Diffusion%20Model/Diffusion%20Model.md)`
2. Notion "공부/개념" 페이지 DB에 새 항목 추가
   - 항목에는 작성 날짜, 개념명, 카테고리, 요약 핵심, GitHub 파일 상대 링크를 기록한다.
3. 연관 개념 정리 확인
   - 새로 배운 연관 개념이나 용어가 추가로 필요한 경우 작성 목록에 등록하거나 보충 서술을 진행한다.
4. git commit 및 git add
   - `git add`까지는 필요시 수행하되, **`git commit`은 작은 단위마다 매번 수행하지 않고 하나의 전체 작업 단위가 완결되었을 때 통합하여 진행**한다.
   - 커밋 타이밍이 모호하거나 확신이 서지 않는 경우에는 사용자에게 커밋 진행 여부를 먼저 확인한다.
   - 지시를 받아 커밋을 진행할 때는 커밋 메시지를 다음 형식으로 작성한다: `docs(concept): {개념명} 정리 추가`
5. push 전 확인
   - **`git push`는 매번 사용자의 명시적인 사전 지시가 있기 전까지 절대 단독으로 실행하지 않으며, 자동 push는 엄격히 금지한다.**

## 5. 작업 체크리스트
개념 정리를 마칠 때마다 아래 항목을 점검한다.
- [ ] 문서가 올바른 경로(`{카테고리}/{개념명}/{개념명}.md` 또는 `{카테고리}/{개념명}/concept.md`)에 저장되었는가?
- [ ] 파일명/폴더명 치환 규칙(특수문자 `_` 변환)을 지켰는가?
- [ ] 상단 Reference 항목에 원문 논문/공식 문서 링크가 명시되었는가?
- [ ] 개념의 등장 배경(Why)과 핵심 원리(How)가 계층적 서브 불렛(`- 키워드` -> `    - 세부 내용`) 구조로 기술되었는가?
- [ ] 수식에 대한 기호/변수 설명이 빠짐없이 작성되었는가?
- [ ] 이미지가 첨부된 경우(로컬 `images/` 또는 온라인 URL) 캡션과 출처(Reference)가 명확히 기재되었는가?
- [ ] 코드 예시 작성 시 한글 docstring, typing, 한글 주석 규칙을 준수했는가?
- [ ] 환각(hallucination)이나 왜곡된 사실 표현이 없는가?
- [ ] README.md에 개념 정리 링크가 추가되었는가?
- [ ] Notion 개념 DB에 항목이 등록되었는가?
- [ ] 전체 작업 단위 완결 후 git commit 전 사용자의 명시적 지시를 확인했는가?
- [ ] push 전 사용자의 명시적 지시를 받았는가? (사용자 요청 시에만 push 실행)

## 6. 링크 작성 기준
- `README.md` 및 파일 간 상호 링크는 항상 저장소 기준의 **상대 경로**로 작성한다.
- 예시:
  - `[Diffusion Model](./Diffusion%20Model/Diffusion%20Model.md)`
- GitHub에서 직접 열람할 수 있는 웹 URL이 있는 경우 함께 기록한다.

## 7. Notion 및 개념 문서 작성 가이드라인
1. **문체 및 어조 (Tone and Style)**:
   - 구어체 종결어미(`~입니다.`, `~합니다.`) 대신 **마침표 없는 간결하고 명료한 명사형 또는 개조식 어조 (`~ 기법`, `~ 모델`, `~을 수행함`, `~를 완화함`)**로 작성한다. (마침표 `.` 생략 필수)
2. **계층적 불렛 레이아웃 구조 (Sub-bullet Layout)**:
   - 단일 길어진 한 문장 불렛 대신, **주요 개념/항목 타이틀을 상위 불렛(`- **타이틀**`)으로 지정하고 하위에 2단계 서브 불렛(`    - 세부 내용`)을 구성하여 계층적으로 작성**한다.
3. **Notion 내용 풍부화 및 표준 템플릿 완비**:
   - 지나치게 간결한 1~2줄 요약을 금지한다.
   - **기존 내용의 보존 및 과도한 요약 금지**: 기존에 잘 정리되어 있던 문서의 유용한 지식, 수식, 서술, 도표, 예시 코드 등을 함부로 삭제하거나 과도하게 압축하지 않는다. 보완/추가 시 기존의 풍부한 설명을 보존하면서 내용을 체계적으로 확충한다.
   - **개념 정의**, **등장 배경 (Why)**, **핵심 원리 및 수식 (How)**, **세부 개념**, **방법**, **장단점 및 비교**, **활용 사례** 등 여러 항목으로 풍부하게 서술한다.
      - 위 항목들 중 필요한 항목을 선택하여 사용한다.
      - 위 항목 외의 필요한 항목이 있다면 추가하여 서술한다.
      - 여러 하위 개념들을 소제목 또는 항목으로 하여 설명할 수 있다.
4. **구조식 및 Notion 수학 공식 블록(Equation) 필수 사용**:
   - 수식을 작성할 때는 단순 텍스트나 코드 블록을 사용하지 않고, **Notion 인라인 수학 공식 (`inline_equation`) 또는 수학 공식 블록 (`equation`)**을 사용하여 LaTeX 수식 표현식으로 깔끔하게 작성한다.
5. **GitHub 웹 URL (`https://github.com/k1101jh/DeepLearning-Concepts`) 배치**:
   - GitHub 개념 노트 파일 경로는 저장소 웹 URL Base(`https://github.com/k1101jh/DeepLearning-Concepts/blob/main/{상대경로}`)를 사용하여 본문 최상단 및 DB Property에 명시하여 깔끔하게 관리한다.
